<% require 'devise/version' %>
<p><%= t('devise.mailer.confirmation_instructions.greeting', recipient: @resource.email) %></p>

<p><%= t('devise.mailer.confirmation_instructions.instruction') %></p>
<p><%= link_to t('devise.mailer.confirmation_instructions.action'), confirmation_url(@resource, confirmation_token: (Devise::VERSION.start_with?('3.') ? @token : @resource.confirmation_token)) %></p>
