<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Photo:</strong>
  <%= @user.photo %>
</p>

<% if policy(@user).edit? %>
  <%= link_to 'Edit', edit_user_path(@user), class: 'btn btn-info btn-xs' %> |
<% end %>
<%= link_to 'Back', users_path, class: 'btn btn-xs' %>
